:time_estimate: 15

= Benchmarking the RAG Search Space

_Estimated reading time: *{time_estimate} minutes*._

Objective::

Prepare evaluation input for AutoRAG and interpret the diagnostic metric triad that scores each retrieval configuration.

.AutoRAG Demo - OpenShift AI 3.5
video::autorag-demo.mp4[width=800,opts=controls]

WARNING: Not started yet.

== Input preparation

AutoRAG is driven by an evaluation JSON dataset. Each test case provides a `question`, `correct_answers` (multiple valid phrasings), and `correct_answer_document_ids` that identify the grounding sources.

== Automated search space and optimization

AutoRAG grid-searches the configuration space that practitioners normally tune by hand:

* Chunking strategy — recursive vs. hybrid structural
* Search mode — vector vs. hybrid
* Fusion strategy — RRF or weighted
* Embedding model — for example, BGE-M3, with multilingual auto-detection

== The diagnostic metric triad

Evaluate each run against three complementary metrics:

* *Answer Faithfulness* — a hallucination check.
* *Answer Correctness* — a ground-truth match.
* *Context Correctness* — retrieval accuracy.

== Production handoff and inspection

Debug pipelines visually with the Pipeline DAG, then export Responses API code snippets (cURL, Python, Go, Node.js) or indexing and inference Jupyter notebooks for handoff.

== What's Next

You have now seen every capability in the course individually. The final chapter synthesizes AutoML, MLflow, and AutoRAG into one end-to-end workflow and validates your understanding with a graded assessment.

////
Input Preparation (Evaluation JSON): Formatting test datasets with `question`, `correct_answers` (multiple phrasings), and `correct_answer_document_ids`. Automated Search Space and Optimization: Grid-searching chunking (recursive vs. hybrid structural), search modes (vector vs. hybrid), strategies (RRF/weighted), embedding models (BGE-M3), and multilingual auto-detection. Diagnostic Metric Triad: Evaluating runs against *Answer Faithfulness* (hallucination check), *Answer Correctness* (ground-truth match), *Context Correctness* (retrieval accuracy). Production Handoff and Inspection: Debugging via the *Visual Pipeline DAG* and exporting *Responses API code snippets* (cURL, Python, Go, Node.js) or Jupyter notebooks.

Session type: Presentation: Breakdown of RAG search space and evaluation metric decision matrix. Arcade / Video Demo: Interactive AutoRAG run clickthrough, comparing leaderboard patterns, and testing in the "Try this pattern" chat panel. Responses API code snippets (cURL, Python, Go, Node.js) or exporting indexing and inference Jupyter notebooks.
////
